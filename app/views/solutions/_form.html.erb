<%= simple_form_for([current_user,@idea,@solution]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :feature %>
    <%= f.input :description %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
